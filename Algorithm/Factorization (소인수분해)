def factorization(n): # 소인수 분해 함수
    x = 2

    if n == 1: # 입력받은 수가 1이라면
        pass
    while x <= n: # 범위 설정
        if n % x == 0: # 나누었을 때 0이 된다면 -> 인수 O
            n /= x # 입력받은 수 / 현재 x값
        else: # 나누었을 때 나머지가 0이 아니라면? -> 인수 X
            x += 1
